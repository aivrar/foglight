[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra --cov=foglight_server --cov=foglight_core --cov-branch --cov-report=term-missing --cov-report=xml:coverage.xml"

[tool.ruff]
target-version = "py313"
line-length = 100

[tool.ruff.lint]
select = ["E4", "E9", "F", "I"]
